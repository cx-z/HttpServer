{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "LLDB: Launch",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/build/HttpServer",
            "args": [],
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "build"
        }
    ]
}